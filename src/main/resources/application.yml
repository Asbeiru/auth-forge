spring:
  datasource:
    url: jdbc:mysql://mysql-dev.ruqimobility.com:13306/ruqi_collect_data?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: gac_travel_dev_new
    password: 'NJElna9OCLisAi#5RfY8oi#M1Qp71ZX'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql

server:
  port: 9999

logging:
  level:
    org.springframework.security: DEBUG
    com.zhouzhou.authforge: DEBUG
